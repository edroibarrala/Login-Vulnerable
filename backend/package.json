{
  "name": "jwt-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "newman run postman-collection.json -e postman-environment.json --reporters cli,json,html",
    "test:config": "newman run --config newman-config.json",
    "test:watch": "nodemon --exec npm run test",
    "test:ci": "newman run postman-collection.json -e postman-environment.json --bail"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "newman": "^6.1.1",
    "newman-reporter-html": "^1.0.5",
    "nodemon": "^3.0.1"
  }
}
